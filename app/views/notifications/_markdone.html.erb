<div class="media-heading">
  <p class="m-0 p-0">
    <strong><%= "#{notification.actor.first_name} #{notification.actor.last_name}" %></strong> has marked the collaboration as <strong><span style="color: #5CDB5C">done</span></strong>.
  </p>
  <p class="m-0 p-0" style="font-size: 12px">Campaign: <strong><%= link_to notification.second_target.name, main_app.campaign_path(notification.second_target) %></strong></p>
  <p class="m-0 p-0" style="font-size: 12px">Collaboration: <strong><%= notification.target.title %></strong></p>
</div>
