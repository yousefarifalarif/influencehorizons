<div class="media-heading">
  <p class="m-0 p-0">
    <strong><%= notification.actor.role == "Business" ? notification.actor.business.company_name : "#{notification.actor.first_name} #{notification.actor.last_name}" %></strong>
    has <span style="color: #5CDB5C">accepted</span> your collaboration under campaign
    <strong><%= link_to notification.second_target.name, main_app.campaign_path(notification.second_target) %></strong>
  </p>
  <p class="m-0 p-0">Collaboration - <strong><%= notification.target.title %></strong></p>
</div>
