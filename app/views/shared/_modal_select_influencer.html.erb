<!-- Create modal -->

  <div class="modal fade" id="selectProposalModal" tabindex="-1" aria-labelledby="selectProposalModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="selectProposalModalLabel"> <%= modal_title %></h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
        <%= render 'influencers/form', { proposal: @proposal, influencer: @influencer } %>
          <%# form with proposals without influencers as dropdown %>
          <%# form should have hidden_field with influencer IDs %>
          <%# user will select one proposal and click submit %>
        </div>
      </div>
    </div>
  </div>
